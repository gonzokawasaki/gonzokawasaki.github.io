{
  "name": "optree-prototype3",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Prototype 3 — the resolver + chips: a local lexicon turns a typed phrase into thin verbs rendered as editable typed chips that write the op-tree (offline, no LLM).",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:resolver": "esbuild test-resolver.ts --bundle --platform=node --format=esm | node -",
    "test:bridge": "esbuild test-bridge.ts --bundle --platform=node --format=esm | node -",
    "test": "npm run test:resolver && npm run test:bridge"
  },
  "dependencies": {
    "@babylonjs/core": "^8.0.0",
    "manifold-3d": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
